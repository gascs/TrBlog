version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: trblog-postgres
    environment:
      POSTGRES_USER: trblog
      POSTGRES_PASSWORD: trblog123
      POSTGRES_DB: trblog
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trblog"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: trblog-redis
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: trblog-backend
    environment:
      DATABASE_URL: "postgresql://trblog:trblog123@postgres:5432/trblog?schema=public"
      REDIS_URL: "redis://redis:6379"
      JWT_SECRET: "your-super-secret-jwt-key-change-this-in-production"
      JWT_EXPIRES_IN: "7d"
      PORT: 3000
      NODE_ENV: "production"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: trblog-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
    name: trblog-postgres-data
  redis_data:
    name: trblog-redis-data
